@model UpdateAboutViewModel
@{
    ViewData["Title"] = "UpdateAbout";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="d-flex align-items-center justify-content-center" style="min-height: 100vh; padding-top: 2rem;">
    <div class="card shadow-sm border-0" style="width: 100%; max-width: 450px; border-radius: 12px;">
        <div class="card-header text-white text-center" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 12px 12px 0 0; padding: 1.25rem;">
            <h5 class="mb-0 text-white fw-semibold"><i class="mdi mdi-car-plus me-2"></i>Hakkımda Güncelle</h5>
        </div>
        <div class="card-body p-3">
            <form id="createAboutForm" method="post">
                <input  type="hidden" asp-for="aboutId"/>
                <div class="mb-2">
                    <label asp-for="title" class="form-label text-dark fw-semibold">İsim-Soyisim</label>
                    <input asp-for="title" class="form-control" placeholder="Örn: 15 CarBook ile Yeni Yolculuk" style="border-radius: 8px;" />
                    <span asp-validation-for="title" class="text-danger"></span>
                </div>
                <div class="mb-2">
                    <label asp-for="description" class="form-label text-dark fw-semibold">Biyografi</label>
                    <input asp-for="description" class="form-control" placeholder="Örn:Yeni ve Modern Araçlarımız Kullanımınıza Hazır" style="border-radius: 8px;" />
                    <span asp-validation-for="description" class="text-danger"></span>
                </div>
                <div class="mb-2">
                    <label asp-for="imageUrl" class="form-label text-dark fw-semibold">Görsel URL</label>
                    <input asp-for="imageUrl" class="form-control" placeholder="Örn:https/Unplash.görsel.jpg" style="border-radius: 8px;" />
                    <span asp-validation-for="imageUrl" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-primary w-100" style="border-radius: 8px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border: none;">
                    <i class="mdi mdi-content-save me-1"></i> Güncelle
                </button>
            </form>
        </div>
        <div class="card-footer bg-light text-center" style="border-radius: 0 0 12px 12px; padding: 0.75rem;">
            <small class="text-muted text-danger d-block">Hakkımda Alanı yalnızca tek bir kayıt içerecek şekilde tasarlanmıştır !</small>
            <small class="text-muted">Birden fazla veri eklenmesi, veri tutarsızlığına neden olabilir!</small>
        </div>
    </div>
</div>
<!-- SweetAlert2 CDN -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    const form = document.getElementById("createAboutForm");
    form.addEventListener("submit", function (event) {
        event.preventDefault(); // formu hemen göndermeyi durdurur
        Swal.fire({
            title: 'Emin misiniz?',
            text: "Bu Şablonu Güncellemek Üzeresiniz!",
            icon: 'question',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Evet, Kaydet',
            cancelButtonText: 'İptal'
        }).then((result) => {
            if (result.isConfirmed) {
                form.submit(); // kullanıcı onaylarsa formu gönder
            }
        });
    });
</script>

