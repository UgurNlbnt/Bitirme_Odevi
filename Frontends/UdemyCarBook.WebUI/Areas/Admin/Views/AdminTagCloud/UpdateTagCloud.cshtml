@model UpdateTagCloudViewModel
@{
    ViewData["Title"] = "UpdateTagCloud";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}


<div class="d-flex align-items-center justify-content-center" style="min-height: 100vh; padding-top: 2rem;">
    <div class="card shadow-sm border-0" style="width: 100%; max-width: 450px; border-radius: 12px;">
        <div class="card-header text-white text-center" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 12px 12px 0 0; padding: 1.25rem;">
            <h5 class="mb-0 text-white fw-semibold"><i class="mdi mdi-car-plus me-2"></i>Etiket Güncelle</h5>
        </div>
        <div class="card-body p-3">
            <form id="createTagForm" method="post">
                <input type="hidden" asp-for="TagCloudId" />
                <div class="mb-2">
                    <label asp-for="Title" class="form-label text-dark fw-semibold">Etiket Adı</label>
                    <input asp-for="Title" class="form-control" placeholder="Örn: Yapay Zeka" style="border-radius: 8px;" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>
                <div class="mb-2">
                    <label asp-for="BlogId" class="form-label text-dark fw-semibold">Blog Id</label>
                    <input type="number" asp-for="BlogId" class="form-control" placeholder="Örn: 1,2,3" style="border-radius: 8px;" />
                    <span asp-validation-for="BlogId" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-primary w-100" style="border-radius: 8px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border: none;">
                    <i class="mdi mdi-content-save me-1"></i> Güncelle
                </button>
            </form>
        </div>
        <div class="card-footer bg-light text-center" style="border-radius: 0 0 12px 12px; padding: 0.75rem;">
            <small class="text-muted text-danger d-block">Etiket Bilgilerini Girerken Lütfen Kontrol Ediniz !</small>
            <small class="text-muted">Bilgileri Girdikten Sonra Kaydet Tuşunu Basmayı Unutmayınız!</small>
        </div>
    </div>
</div>
<!-- SweetAlert2 CDN -->
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    const form = document.getElementById("createTagForm");
    form.addEventListener("submit", function (event) {
        event.preventDefault(); // formu hemen göndermeyi durdurur
        Swal.fire({
            title: 'Emin misiniz?',
            text: "Bu Etiketi Güncellemek Üzeresiniz!",
            icon: 'question',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Evet, Kaydet',
            cancelButtonText: 'İptal'
        }).then((result) => {
            if (result.isConfirmed) {
                form.submit(); // kullanıcı onaylarsa formu gönder
            }
        });
    });
</script>
