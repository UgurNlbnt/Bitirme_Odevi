

<div class="sidebar-box ftco-animate">
    <h3>Etiket Bulutu</h3>
    <div class="tagcloud">
        @foreach (var tag in Model)
        {
            <a href="#" class="tag-cloud-link">@tag.Title</a>
        }
    </div>
</div>